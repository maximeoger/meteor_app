import React from 'react';
import styled from 'styled-components';
import { app_bg } from '../vars/vars';

const Card = styled.div`
  background-color: ${app_bg};
  padding: 10px;
  font-family: "Lato", sans-serif;
`;

export default Card;